<!-- BEGIN: Layout -->
<%= render :layout => 'outershell' do %>
  <div class="row">
    <div class="small-12 columns">
      <div class="workflow-panel">
        <div class="workflow-title clearfix">
          <div class="text left">Set Clips</div>
        </div>
        <div class="workflow-content">

<!-- BEGIN: Content -->
<%= simple_form_for @mining, :url => wizard_path, :method => :put do |f| %>

<p>TODO: change once we have better way of handling continuous run clips.</p>

<%= f.select :capture_ids, options_for_select(@workflowObj.formattedCaptures.map{ |c| [c[:captureName], c[:captureId], c[:isSelected] ? {'data-section' => c[:streamName], 'data-index' => c[:idx], 'selected' => 'selected'} : {'data-section' => c[:streamName], 'data-index' => c[:idx]}] }), {:multiple => true}, id: "channel_selection", name: "capture_ids[]" %>

<p></p>


<%= render 'common_buttons', :form_obj => f %>
<% end %>
<!-- END: Content -->

        </div>
      </div>
    </div>
  </div>
<% end %>
<!-- END: Layout -->
