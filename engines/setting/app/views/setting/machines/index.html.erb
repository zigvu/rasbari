<h3>Listing Machines</h3>

<table>
  <thead>
    <tr>
      <th>Ztype</th>
      <th>Zstate</th>
      <th>Cloud</th>
      <th>Hostname</th>
      <th>Ip</th>
      <th>Zdetails</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @machines.each do |machine| %>
      <tr>
        <td><%= machine.ztype %></td>
        <td><%= machine.zstate %></td>
        <td><%= machine.cloud %></td>
        <td><%= machine.hostname %></td>
        <td><%= machine.ip %></td>
        <td><%= machine.zdetails %></td>
        <td><%= link_to 'Show', machine %></td>
        <td><%= link_to 'Edit', edit_machine_path(machine) %></td>
        <td><%= link_to 'Destroy', machine, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Machine', new_machine_path, class: 'button' %>
